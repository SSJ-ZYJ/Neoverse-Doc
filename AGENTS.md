# AGENTS.md

## 代码增改准则

### 增改前

1. **遵循最小改动原则**

   * 应优先采用最小范围的修改方案。
   * 不得无故重构、移动或修改与当前任务无关的代码。
   * 应尽量避免对现有功能、接口和行为产生额外影响。

2. **优先复用现有实现**

   * 在实现新功能前，应先检查项目现有代码及 `fumadocs` 官方文档，确认是否已经存在对应能力或推荐实现。
   * 禁止重复造轮子，优先使用项目现有组件、API、工具函数和框架能力。

3. **遵循项目既有技术栈**

   * 必须以项目 `package.json` 中声明的依赖及版本为准。
   * 必须遵循项目现有的 ESLint、Biome 及其他代码规范。
   * 禁止擅自升级、降级或替换已有依赖。
   * 如确需引入新的外部依赖，应先向用户说明依赖名称、用途及拟使用版本，并在获得确认后再进行修改。

4. **禁止硬编码用户可见文本**

   * 所有新增或修改的用户可见文本必须接入项目现有 i18n 本地化体系。
   * 禁止直接在组件、页面或业务逻辑中硬编码中文、英文或其他用户可见文本。
   * 中文文本应提取至项目现有的本地化资源中，并按照现有目录结构与命名方式维护。

5. **新增代码应补充必要注释**

   * 对新增的核心逻辑、非直观实现或存在设计意图的代码，应添加简洁、准确的功能说明注释。
   * 代码注释保持中英双语习惯：英文在上，中文在下。
   * 不得添加无意义、重复代码本身含义或过度解释的注释。
   * 本规则仅适用于代码，不适用于文档正文。

6. **文档同步要求**

   * 若修改内容涉及 `content/` 下现有的多语言文档，应保持对应语言版本内容一致。
   * 对当前仅提供简体中文的文档，不得为了满足形式上的同步要求擅自创建英文版本。
   * 后续已有英文版本的文档发生变更时，应同步维护中文与英文内容。

7. **不确定时先确认**

   * 若需求存在多种明显不同的实现方式，或修改可能影响现有架构、依赖、数据结构、公共 API、用户数据或兼容性，应先向用户确认。
   * 对能够从现有代码、配置或官方文档中明确判断的事项，应自行确认，不应重复询问用户。

8. **禁止擅自执行 Git 提交**

   * Agent 可以查看 Git 状态、Diff 和历史记录，但不得自行执行任何提交或推送操作。
   * 禁止执行包括但不限于以下操作：

     * `git commit`
     * `git push`
     * 向主分支直接提交
     * 创建或合并 Pull Request
     * 强制推送
   * 修改完成后，仅提供建议的 Git Commit Message。

## 增改后

1. **整理代码**

   * 检查并清理无用导入、重复逻辑、调试代码和临时实现。
   * 保持代码结构、命名方式和格式与项目现有风格一致。

2. **检查本地化**

   * 检查新增及修改内容是否存在用户可见文本硬编码。
   * 如存在，应迁移至项目现有 i18n 本地化体系。

3. **检查功能影响**

   * 若修改涉及功能逻辑，应确认相关改动不会破坏现有功能。
   * 应重点检查受修改代码直接影响的调用方、组件、接口和数据流。

4. **同步相关文档**

   * 若功能、配置、命令、目录结构、接口或使用方式发生变化，应同步检查并更新相关文档。
   * 根据实际影响范围维护：

     * 项目文档
     * `README.md`
     * `CONTRIBUTING.md`
     * 其他与该功能直接相关的文件
   * 不得为了满足此规则而修改与当前变更无关的文档。

5. **执行检查**

   * 修改完成后，应提示用户运行：

   ```bash
   bun format
   bun check
   ```

   * 若当前环境允许且用户未限制测试范围，可根据实际修改内容执行必要的静态检查或测试。
   * 若用户明确说明测试由其负责，则：

     * 不得执行构建；
     * 不得执行类型检查；
     * 不得执行浏览器自动化；
     * 不得执行其他测试；
     * 仅可运行用户明确指定的静态检查命令，例如 `bun check`。
   * 所有检查结果必须如实报告，不得声称未实际执行的检查已经通过。

6. **生成 Git Commit Message**

   * 修改完成后，应根据 `.vscode/prompt/commit-instruction.md` 的规范生成 Git Commit Message。
   * 仅输出建议的 Commit Message，不得执行实际提交操作。

## 文档编写准则

### 文档命名规范

1. 文档及相关目录名称原则上使用英文，并应准确反映其内容。

2. 多单词文件名使用下划线 `_` 分隔，例如：

   ```text
   README_en.md
   getting_started.mdx
   project_structure.mdx
   ```

3. 文件名大小写敏感，应严格保持引用路径与实际文件名一致。

4. 已存在且遵循其他命名约定的文件，不应仅为统一风格而无必要重命名。

### 文档语言策略

1. 当前阶段所有新增文档默认仅提供**简体中文**版本。

2. 后续补充英文版本时，统一使用 `_en` 后缀，例如：

   ```text
   README_en.md
   ARCH_en.mdx
   ```

3. 对已经存在中英文双语版本的文档，修改任一版本时应同步维护另一版本，确保核心信息一致。

4. 代码注释沿用中英双语习惯：

   * 英文在上；
   * 中文在下。

### 文档内容规范

1. 文档中的命令、环境变量、配置项、文件路径、目录结构和功能描述必须与当前代码实现保持一致。
2. 禁止描述：

   * 尚未实现的能力；
   * 已删除的功能；
   * 已废弃但未明确标注的行为；
   * 无法从当前项目状态确认的功能。
3. 涉及外部依赖时，应明确依赖名称；若存在最低版本要求，应同时注明版本下限。
4. 技术表述应准确、简洁，避免使用容易过时、过度绝对或无法验证的描述。
5. 文档正文中不添加用于说明“此次修改了什么”的开发注释、更新注释或变更标记；相关变更信息应交由 Git Commit、Changelog 或版本记录承担。

### 文档格式规范

1. 项目文档统一使用 `.mdx` 格式；已有特殊用途的 Markdown 文件，如 `README.md`、`CONTRIBUTING.md`，保持其现有格式。

2. 文档正文标题从 H2（`##`）开始，不额外添加 H1，除非现有文档结构或框架明确要求。

3. 流程图、时序图及适合图形化表达的结构关系优先使用 Mermaid，并确保图表清晰、语法有效。

4. 所有代码块必须声明合适的语言类型，例如：

   ```bash
   bun install
   ```

5. 中文与英文、数字、英文缩写之间原则上使用一个半角空格，例如：

   ```text
   使用 Windows 11 安装 Node.js 运行环境。
   ```

6. 中文文字与中文全角标点之间不得额外添加空格，例如：

   ```text
   正确：安装完成后，重新启动程序。
   错误：安装完成后 ，重新启动程序 。
   ```

7. 英文关键字、命令、配置项、环境变量、文件名、目录名和路径等技术标识，应根据语境使用反引号包裹，例如：

   ```text
   运行 `bun check` 检查项目，并确认 `package.json` 中的依赖配置。
   ```

8. 不应滥用反引号。普通英文专有名词、产品名称和自然语言内容无需强制包裹。

## 执行原则

当不同规则发生冲突时，按照以下优先级处理：

1. 用户当前明确提出的要求；
2. 项目现有代码、配置及实际实现；
3. 本 `AGENTS.md`；
4. 相关框架及依赖的官方文档；
5. 通用工程实践。

任何情况下，都不得为了机械满足规则而引入无关修改、破坏现有行为或虚构项目中不存在的能力。
