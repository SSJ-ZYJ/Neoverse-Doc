// Docs shell feature: product-level composition over the Fumadocs docs page,
// including deferred TOC retention across route transitions. The Fumadocs
// adapters stay framework-facing; this feature owns the product behavior.
// 文档外壳特性：在 Fumadocs 文档页之上进行产品级组合，包含路由切换期间
// 的 TOC 保留行为。Fumadocs 适配器只面向框架；本特性拥有产品行为。
export { DeferredDocsPage } from './deferred-docs-page';
